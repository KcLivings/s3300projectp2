
<nav class="navbar navbar-expand-lg navbar-dark bg-black">
  <div class="container-fluid">
    <%= link_to 'NAMEOFWEBSITE', items_path, class:"homelink navbar-brand" %>
    
    <a class="nav-link dropdown-toggle text-white" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
      Account
    </a>
    <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdown">
      <li><a class="dropdown-item" href="#">Login</a></li>
      <li><a class="dropdown-item" href="#">Sign Up</a></li>
    </ul>
  </div>
</nav>

<p id="notice"><%= notice %></p>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Description</th>
      <th>Price</th>
      <th>Brand</th>
      <th>Mousestyle</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @items.each do |item| %>

      <div class="container-sm border p-3 bg-dark text-white">
        <img class="rounded" src="s3300projectp2/app/assets/images/8357047_R_Z001A.jpg" alt="mouseimage"> 
        <h2> <%= item.title %> </h2>
        <p> <%= item.description %> </p>
        <p> <%= item.brand %> </p>
        <p> <%= item.mousestyle %> </p>
        <p> <%= item.price %> </p>
        
        
        <%= link_to 'Show', item %>
        <%= link_to 'Edit', edit_item_path(item) %>
      </div>

      <!--
      <tr>
        <td><%= item.title %></td>
        <td><%= item.description %></td>
        <td><%= item.price %></td>
        <td><%= item.brand %></td>
        <td><%= item.mousestyle %></td>
        <td><%= link_to 'Show', item %></td>
        <td><%= link_to 'Edit', edit_item_path(item) %></td>
      </tr> -->
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Item', new_item_path %>
